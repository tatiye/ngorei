import tatiyeNet,{
modalEntri,
mainRouterId,
modalRouteFrom,
useHandel,
useTabPages,
useHandelID,
setModal
}  from "../es6.js"; 
  let count = 0;
 // import '../pagination.min.js';
export function setModalStandar(read) {
          count++;
   // console.log(read)

       let attrApp=read.app;
       let attribute=read.element;
       const getAtr = document.querySelector(attribute);
        let setID=getAtr.getAttribute('indexOn');
        let useConsole=getAtr.getAttribute('console');
       const elemenID = document.querySelector(setID);
        let setIDKeyLimit=elemenID.getAttribute('key');
        let setIDKeyPage=elemenID.getAttribute('page');
        let setIDKeyWidth=elemenID.getAttribute('width');
      var resheader =read.title.replace(/[^A-Za-z0-9]/g, '')+count;
      if (read.property[0]=='target') {
      var useElement='#'+read.property[1];
      } else {
      var useElement='#body'+resheader;
      
      setModal({
           'titel':read.title,
           'width':setIDKeyWidth,
           'key'  :read.property[1],
           'model':resheader,
           'route':'route',
           'tabel':'tabel',
           'content':'fromAction',
           'element':'get',
           'page'   :1,
           'setdata' :'public',
          });
        }
       let dabatabse=useHandel({
           "token"    :read.token,
           "storage"       :{
              "limit":setIDKeyLimit,
              "page":setIDKeyPage,
              "keywords":read.keywords
           }
        })
      if (useConsole=='true') {
            console.log(dabatabse)
        }
  var myTemplate='';
      dabatabse.storage.forEach(row => {
            $(setID+" a").each(function( index ) {
                  $(this).attr('id',row.id)
                  $(this).attr('name')
                 
                  $(this).attr("href", 'javascript:void(0);');
                  $(this).removeAttr('app')
             });        
             $(setID+" h1").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             });

             $(setID+" h2").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             });
             $(setID+" h3").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             });
             $(setID+" h4").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             });
             $(setID+" h5").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             });
             $(setID+" h6").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             });        
             $(setID+" div").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             }); 
             $(setID+" p").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             });    
             $(setID+" li").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             }); 

            $(setID+" dt").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             }); 
            $(setID+" dd").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             }); 
            $(setID+" b").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             }); 

            $(setID+" strong").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             }); 
            $(setID+" small").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             }); 
            $(setID+" class").each(function( index ) {
                 if ($(this).attr('name')) {
                  $(this).attr('id',$(this).attr('name')+index+row.no)
                 }
             });          


              $(setID+" span").each(function( index ) {
                    if ($(this).attr('name')) {
                     $(this).attr('id',$(this).attr('name')+index+row.no)

                    }
                });


                $(setID+" dropdown").each(function(index) {
                    if ($(this).attr('name')) {
                        $(this).attr('id','down'+row.no)
                        $(this).attr('name',row.id)
                    }
                });

              myTemplate +=$(setID).html();
             
            });
              $(useElement).html(myTemplate)
             //console.log(myTemplate)



           /*
            |--------------------------------------------------------------------------
            | Initializes Database 
            |--------------------------------------------------------------------------
            | Develover Tatiye.Net 2018
            | @Date 
            */
            /**
             * @param array  options the display options .
             * @param mixed  Block to generate a customized inside  content.
             */
           dabatabse.storage.forEach(set => {
           
                 $(setID+"  h1").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                });
                $(setID+"  h2").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                });
                 $(setID+"  h3").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                });
                $(setID+"  h4").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                });
                    $(setID+"  h5").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                });
                $(setID+"  h6").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                });
                 $(setID+"  span").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      } 

                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                      // $(this).removeAttr('name')

                   }
                });

                 $(setID+"  p").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                });
                 $(setID+" div").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                }); 

                 $(setID+"  li").each(function( index ) {
                   if ($(this).attr('name')) {
                      if ($(this).attr('name') !=='id') {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);

                      }
                   }
                }); 
          
                $(setID+"  dt").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                });
                    $(setID+"  dd").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                });
                $(setID+"  b").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                });
                 $(setID+"  strong").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                });

                 $(setID+"  small").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                });
                 $(setID+"  class").each(function( index ) {
                   if ($(this).attr('name')) {
                      var myId=$(this).attr('name')+index+set.no;
                      var myIdname=$(this).attr('name');
                      var myText=$(useElement +" #"+myId).html();
                      if ($(this).attr('textRight')) {
                         var textRight=$(this).attr('textRight');
                      } else {
                          var textRight='';
                      }
                      if ($(this).attr('textClass')) {
                         var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                      } else {
                          var textValue=set[myIdname];
                      }  
                      $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                   }
                }); 

            });









};
