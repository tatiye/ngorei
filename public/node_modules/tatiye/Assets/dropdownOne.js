import tatiyeNet,{modalEntri,mainRouterId,modalRouteFrom}  from "../es6.js"; 
export function setDropdown(row) {

    let attribute=row.app;
    let element=row.element;
    let onclick=row.onclick;
    // //var myId='#'+$(row.attribute).attr('id');
    // var myId=$(row.attribute).attr('id');
    

       //const seEl = document.querySelector(attribute); 
      $(element).each(function(index) {
        // if (index !==0) {
            var myID=$( this ).attr('id');
            var mynameID=$( this ).attr('name');
              var setdropdown='';
                 $.each(row.lengthMenu,function(keydwn,dwn){
 
                    if (dwn[0]=='download') {

                      setdropdown +=`<a  href="${typeDevices}" class="pt-5px dropdown-item"target="_blank">
                       <i class="icon-feather-${dwn[2]} fs-14px mr-5px"></i> ${keydwn}</a>`;
                    } else {
                      setdropdown +=`<a  onclick="${onclick}(['${dwn[0]}','${mynameID}','${dwn[1]}','${dwn[3]}']);" href="javascript:void(0);" class="pt-5px dropdown-item ">
                       <i class="feat feat-${dwn[2]} fs-14px mr-5px"></i> ${keydwn}</a>`;
                    }
    
                  });
                var desainTabel=`
                 <a href="javascript:void(0);" class="dropdown-link" data-toggle="dropdown"><i class="feat feat-more-vertical"></i></a>
                 <div class="dropdown-menu">
                     ${setdropdown}
                 </div>
                `;
            $(element +'#'+myID).html(desainTabel);


      });

  
};
