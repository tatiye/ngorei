import tatiyeNet,{
modalEntri,
mainRouterId,
modalRouteFrom,
useHandel,
useTabPages,
readmore,
components
}  from "../es6.js"; 
import '../pagination.min.js';
export function renderElements(query,setID,useElement,keyname='name') {
         var myTemplate='';
        $(query).each(function( index,row ) {
            $(setID+" a").each(function( index ) {
                    if ($(this).attr('link')) {
                        $(this).attr('id',$(this).attr('link')+index+row.id)
                        $(this).attr("href", row.posts);
                    }
                   if ($(this).attr('onclick')) {
                        $(this).attr('id',row.id)
                        ///$(this).attr(keyname) 
                        $(this).attr("href", 'javascript:void(0);');
                        ///$(this).removeAttr('app')
                    }
             }); 

             $(setID+" h1").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             });

             $(setID+" h2").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             });
             $(setID+" h3").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             });
             $(setID+" h4").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             });
             $(setID+" h5").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             });
             $(setID+" h6").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             });        
             $(setID+" div").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             }); 
             $(setID+" p").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             });    
             $(setID+" li").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             }); 

            $(setID+" dt").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             }); 
            $(setID+" dd").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             }); 
            $(setID+" b").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             }); 

            $(setID+" strong").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             }); 
            $(setID+" small").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             }); 
            $(setID+" class").each(function( index ) {
                 if ($(this).attr(keyname)) {
                  $(this).attr('id',$(this).attr(keyname)+index+row.id)
                 }
             });          

              $(setID+" img").each(function( index ) {
                    if ($(this).attr(keyname)) {
                     $(this).attr('id',$(this).attr(keyname)+index+row.id)
                    }
                });

              $(setID+" i").each(function( index ) {
                    if ($(this).attr(keyname)) {
                     $(this).attr('id',$(this).attr(keyname)+index+row.id)
                    }
                });
              $(setID+" span").each(function( index ) {
                    if ($(this).attr(keyname)) {
                     $(this).attr('id',$(this).attr(keyname)+index+row.id)
                    }
                });

                $(setID+" dropdown").each(function(index) {
                    if ($(this).attr(keyname)) {
                        $(this).attr('id',$(this).attr(keyname)+index+row.id)
                    }
                });

                $(setID+" usedevices").each(function(index) {
                    if ($(this).attr(keyname)) {
                        $(this).attr('id',$(this).attr(keyname)+index+row.id)
                    }
                });
                 $(setID+" usedevicesxlsx").each(function(index) {
                    if ($(this).attr(keyname)) {
                        $(this).attr('id',$(this).attr(keyname)+index+row.id)
                    }
                });
               myTemplate +=$(setID).html();
        });
       
         $(useElement).html(myTemplate)

                      $(query).each(function( index,set ) {

                           $(setID+"  dropdown").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              app.dropdown({
                                "app":useElement +" #"+myId,
                                "lengthMenu":lengthMenudropdown,
                                
                                "key":set[myIdname]
                              })
                              // 
                              // $(useElement +" #"+myId).html()
                           }
                        });
                    
                     $(setID+"  usedevices").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              app.setDevices({
                                "app":useElement +" #"+myId,
                                "lengthMenu":lengthMenudevices,
                                "token":token,
                                "key":set[myIdname]
                              })
                              // 
                              // $(useElement +" #"+myId).html()
                           }
                        });
    $(setID+"  img").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              $(useElement +" #"+myId).attr("src", set[myIdname]);
                              
                           }
                        });

                         $(setID+"  h1").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              }  if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              }
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        });
                        $(setID+"  h2").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              }  if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              }
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        });
                         $(setID+"  h3").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              }  if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              }
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        });
                        $(setID+"  h4").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              }  if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              }
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        });
                            $(setID+"  h5").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              }  if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              }
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        });
                        $(setID+"  h6").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              }  if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              }
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        });
                         $(setID+"  span").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              } if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              }

                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                              // $(this).removeAttr(keyname)

                           }
                        });

                         $(setID+"  p").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              }  
                              if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              }
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        });
                         $(setID+" div").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              }  if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              }
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        }); 

                         $(setID+"  li").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              if ($(this).attr(keyname) !=='id') {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              }  if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              }
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);

                              }
                           }
                        }); 
                  
                        $(setID+"  dt").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              } if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              } 
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        });
                            $(setID+"  dd").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              } if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              } 
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        });
                        $(setID+"  b").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              } if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              } 
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        });
                         $(setID+"  strong").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              } if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              } 
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        });

                         $(setID+"  small").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              } if ($(this).attr('textMore')) {
                                 var textValue=readmore(set[myIdname],$(this).attr('textMore'));
                              } else {
                                  var textValue=set[myIdname];
                              } 
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        });

                      $(setID+"  class").each(function( index ) {
                           if ($(this).attr(keyname)) {
                              var myId=$(this).attr(keyname)+index+set.id;
                              var myIdname=$(this).attr(keyname);
                              var myText=$(useElement +" #"+myId).html();
                              if ($(this).attr('textRight')) {
                                 var textRight=$(this).attr('textRight');
                              } else {
                                  var textRight='';
                              }
                              if ($(this).attr('textClass')) {
                                 var textValue='<tx class="'+$(this).attr('textClass')+'">'+set[myIdname]+'</tx>';
                              } else {
                                  var textValue=set[myIdname];
                              }  
                              $(useElement +" #"+myId).html(myText+' '+textValue+' '+textRight);
                           }
                        }); 

                         $(setID+" i").each(function( index ) {
                           if ($(this).attr(keyname)) {
                               var myId=$(this).attr(keyname)+index+set.id;
                               var myIdname=$(this).attr(keyname);
                               $(useElement +" #"+myId).attr('class',set[myIdname]);
                           }
                        }); 
  });
 //   var selectorutilities=document.querySelector(id);
 //    set.forEach(set => {
 //           // $.each(selectorutilities,function(index){
 //           //  console.log(index)
 //         // Height
 //         // if ($(this).attr('ht')) {
 //         //    $(this).attr('id','ht'+index);
 //         //    var ht=$(this).attr('ht');
 //         //    var sizeId=$(this).attr('id');
 //         //    $("#"+sizeId).css({'height': ht});
 //         //   }

 // });
    // console.log(selectorutilities)
    // console.log(set)
   


};
